// import { updateUser } from '~/server/services/user/userService'

// export default defineEventHandler(async (event) => {
//   const body = await readBody(event)
//   return await updateUser(body.no, body.name, body.email)
// })